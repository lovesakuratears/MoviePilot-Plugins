# Checklist

## 插件基础
- [x] 插件在 MoviePilot V2 中可正常注册和显示
- [x] `package.v2.json` 结构正确，包含版本历史
- [x] 清除历史记录改为开关（保存设置后执行，自动关闭）
- [x] 豆瓣页面抓取补充数据（海报/评分/集数/单集片长/日期）正常
- [x] 豆瓣订阅三级回退（RSS → SubscribeChain → add_subscribe）正常
- [x] 开播前24小时提醒注册正常
- [x] 播放平台搜索（Bing）正常
- [x] 定档时间搜索（Bing）正常
- [x] 搜预告按钮（跳转抖音）正常
- [x] 通知包含播放平台信息
- [x] 通知包含四个交互按钮（查看详情、搜预告、有兴趣、无兴趣）
- [x] 追踪刷新增加定档时间搜索和豆瓣订阅尝试
- [x] 版本更新至 v1.2.0

## 配置表单
- [x] 所有配置字段可正常渲染（VSwitch、VSelect、VTextField、VBtn）
- [x] 数据源多选（即将上映/实时热门）正常工作
- [x] 地区多选（中国大陆/海外）正常工作
- [x] 推送数量输入范围限制（默认 10，最大 20）
- [x] 排序方式选择（热度/时间）正常工作
- [x] 推送时间输入（默认 09:00）正常工作
- [x] 清除历史记录按钮功能正常

## 数据获取
- [x] 即将上映路由 (`/douban/tv/coming`) 返回有效数据
- [x] 实时热门路由 (`/douban/list/tv_real_time_hotest`) 返回有效数据
- [x] 我想看路由（`/douban/recommended/tv`）返回有效数据
- [x] RSS 解析正确提取 title、douban_id、year、region、genres、director、actors、summary
- [x] 实时热门路由正确提取图片 URL 和评分
- [x] 多数据源合并去重逻辑正确

## 定时推送
- [x] 每日推送按配置时间执行
- [x] 推送流程：获取 → 去重 → 筛选 → 排序 → 取 N 条 → 推送

## 通知格式
- [x] 通知包含图片（海报）
- [x] 通知文本格式：🎞 标题(年份) / ✨ 评分 / 👾 主演 / 播出时间 / 播放平台 / 🔗 链接 / 🍿 简介
- [x] 通知包含四个交互按钮（查看详情、搜预告、有兴趣、无兴趣）

## 交互按钮
- [x] "查看详情"按钮可跳转到豆瓣页面
- [x] "搜预告"按钮可跳转抖音搜索预告
- [x] "有兴趣"按钮触发 TMDB 匹配订阅流程
- [x] TMDB 匹配成功且有精确时间 → 添加订阅 + 日历 + 24h提醒
- [x] TMDB 匹配成功但无精确时间 → 本地追踪
- [x] TMDB 匹配失败 → 豆瓣订阅（三级回退）→ 本地追踪
- [x] "无兴趣"按钮跳过当前条目，推送下一条

## 去重与历史
- [x] 已推送条目不再重复推送
- [x] 历史记录页面：感兴趣默认展开
- [x] 历史记录页面：不感兴趣默认折叠，可展开
- [x] 清除历史记录功能正常

## 筛选与过滤
- [x] 地区筛选（中国大陆/海外）正确过滤
- [x] 短剧过滤（≤ 10 分钟）正确剔除（占位实现，RSS数据中无单集片长字段）

## 本地追踪
- [x] 本地追踪条目每日刷新
- [x] 匹配成功自动订阅，从追踪列表移除
- [x] 匹配失败保留，次日继续

## 通知去重
- [x] 同标题通知不重复发送

## 插件重命名（v1.1.0）
- [x] 插件名称改为"刷豆瓣助手"
- [x] `plugin_desc` 更新为一条龙服务描述
- [x] `package.v2.json` name/description 同步更新
- [x] `package.v2.json` 版本历史添加 v1.1.0

## 配置表单更新
- [x] 数据来源仅保留"即将上映"和"实时热门"（移除"我想看"）
- [x] 新增"豆瓣UID"输入框
- [x] 新增"豆瓣想看"开关
- [x] 新增 `douban_uid` 和 `enable_wish` 配置字段持久化
- [x] 提示信息说明豆瓣UID和想看功能

## 豆瓣想看功能
- [x] `__fetch_douban_wish` 通过 RSSHub 获取用户想看列表
- [x] 仅处理未处理条目（跳过已处理）
- [x] 未上映条目自动 TMDB 匹配订阅（优先）
- [x] 匹配失败走豆瓣订阅 → 本地追踪
- [x] 已处理条目记录到 `_pushed_items` 避免重复
- [x] `__run_push` 末尾调用豆瓣想看处理（如果开启）
- [x] 豆瓣想看刷新服务注册（每 12 小时）
- [x] 首次启动延迟 5 分钟后执行首次想看处理

## v1.3.0 修复与新增
- [x] 插件未启用时立即执行开关不触发推送
- [x] 豆瓣想看列表改用官方 feed `https://www.douban.com/feed/people/{uid}/interests`
- [x] 播放平台搜索误判已修复（仅匹配搜索摘要文本）
- [x] "停止"按钮工作正常：清空 `_current_queue` 并结束本轮推送
- [x] 通知按钮改用 `buttons` + `callback_data` 格式
- [x] 兼容多种 `callback_data` 解析（标准/简写/最简）
- [x] 播放平台优先从 TMDB `watch/providers` 获取，无结果回退 Bing
- [x] 版本更新至 v1.3.0

## v1.4.0 修复与优化
- [x] TMDB 导入路径使用 `from app.chain.tmdb import TmdbChain`
- [x] 订阅 `mtype` 使用 `MediaType.TV` / `MediaType.MOVIE` 枚举
- [x] "自动订阅想看"开关默认关闭
- [x] 启用速度优化：仅对前 `push_count` 条抓取豆瓣详情
- [x] 首次想看处理延迟 10 分钟（仅自动订阅开启时触发）
- [x] 播放平台行无前置空格
- [x] 版本更新至 v1.4.0

## v1.5.0 UI 卡片式布局
- [x] `get_page()` 改为 VListItem + 海报 + 评分卡片式布局
- [x] 推送记录持久化海报/评分/年份/类型等详情
- [x] 空状态展示大图标 + 提示文字
- [x] 版本更新至 v1.5.0

## v1.6.0 网格卡片与通知精简
- [x] 清除历史记录开关执行后通过 `__save_config()` 持久化重置
- [x] 立即执行与首次推送定时任务去重，通知不再重复
- [x] `get_page()` 重构为 `VGrid` 网格卡片布局（xs 2 / sm 3 / md 4 / lg 5 / xl 6）
- [x] 卡片时间显示预计播出日期（无则显示推送时间）
- [x] 通知文本评分行由播放平台代替（带 ✨ 前缀），保留简介
- [x] 通知链接优先使用 TMDB 链接（无则豆瓣）
- [x] 通知海报优先使用 TMDB 海报（无则豆瓣）
- [x] `package.v2.json`（插件目录 + 根目录）与 `plugin_version` 同步更新到 1.6.0